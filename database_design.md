## Deployment Consideration (AWS)

When deploying to AWS:
- The database can be hosted locally first
- Later migrated to AWS RDS for better scalability
- Connection strings will be updated for cloud access

This approach ensures smooth transition from local development to cloud deployment.
